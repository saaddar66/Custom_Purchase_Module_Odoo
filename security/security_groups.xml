<odoo>
    <data>

        <record id="category_custom_purchase" model="ir.module.category">
            <field name="name">Custom Purchase Access</field>
        </record>

        <record id="group_purchase_approver" model="res.groups">
            <field name="name">Purchase Approver</field>
            <field name="category_id" ref="category_custom_purchase"/>
            <field name="implied_ids" eval="[(4, ref('purchase.group_purchase_manager'))]"/>
        </record>

        <!-- Record Rules -->
        <record id="purchase_order_user_rule" model="ir.rule">
            <field name="name">Personal or Departmental Purchase Orders</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="domain_force">['|', ('requested_by', '=', user.id), ('department_id', 'in', user.employee_ids.department_id.ids)]</field>
            <field name="groups" eval="[(4, ref('purchase.group_purchase_user'))]"/>
        </record>

        <record id="purchase_order_manager_rule" model="ir.rule">
            <field name="name">All Purchase Orders (Manager/Approver)</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('purchase.group_purchase_manager')), (4, ref('custom_purchase_module.group_purchase_approver'))]"/>
        </record>

    </data>
</odoo>